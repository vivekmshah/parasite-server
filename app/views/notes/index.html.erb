<%@notes.each do |note|%>
	<%= link_to "#{note.description}", note_path(note) %><br>
<% end %>

<% if current_user %>

  <%= form_tag notes_path do %>
    <%= text_field_tag :description %>
    <%= submit_tag "submit" %>
  <% end %>

<% end %>