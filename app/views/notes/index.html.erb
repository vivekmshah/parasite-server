<%@notes.each do |note|%>
	<%= note.description%><br>
<% end %>
<% if current_user%>
	<%= form_for(@note,{action: :create, controller: :notes}) do |f| %>
          <%= f.text_field :description, :placeholder =>  "description"%>
          <%= f.submit "submit"%>
    <% end %>
<% end%>